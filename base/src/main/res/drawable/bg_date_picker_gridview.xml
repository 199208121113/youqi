<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >

    <!-- 填充的颜色 -->
    <solid android:color="@color/date_picker_bg_border"/>
    
    <!-- 圆角  android:radius 弧形的半径 
    <corners android:radius="10dp"  />
    -->
    <!-- 描边 边框宽度及颜色 -->
    <stroke android:width="1dp" android:color="@color/date_picker_bg_border" />

    <!-- 内边距间隔 -->
    <padding android:left="0dp" android:top="0dp" android:bottom="0dp" android:right="0dp"/>

</shape>